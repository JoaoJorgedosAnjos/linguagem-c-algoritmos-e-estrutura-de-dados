#include <stdio.h>

int main()
{
    int x = 10, y = 10, z = 2;
    x = x + z;
    y += z; // y = y + z;
    /*
    y += z; y = y + z;
    y -= z; y = y - z;
    y *= z; y = y * z;
    y /= z; y = y / z;
    y %= z; y = y % z;
    */
    return 0;
}